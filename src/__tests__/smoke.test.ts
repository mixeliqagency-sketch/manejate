// Test de humo para verificar que el entorno de testing funciona
import { describe, it, expect } from 'vitest'

describe('setup del entorno de testing', () => {
  it('vitest funciona correctamente', () => {
    expect(true).toBe(true)
  })
})
